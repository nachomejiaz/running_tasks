RUNNING_TASK SOURCE VALIDATION
==============================
Version: 1.0.0-rc.1
Validation date: July 27, 2026
Branch: release/v1.0.0-rc.1

RESULT
------
PASS - all source-level checks available in the current Linux environment.

REPEATABLE COMMAND
------------------
npm run verify

PASSED
------
1. TypeScript compilation.
2. Self-contained browser-preview generation.
3. Compiled JavaScript syntax validation.
4. Static product, route, styling, local-asset, and configuration checks.
5. Live SQLite schema execution and expected-table checks.
6. Existing domain rules for next action, BIC, Calendar, progress, Done,
   Archive, JSON import, CSV export, and Markdown export.
7. Serialized-save ordering and coalescing.
8. Failed-save retention and successful explicit retry.
9. Fail-closed desktop load recovery with no replacement workspace queued.
10. Recursive React rendering for all primary views, overlays, and recovery UI.
11. Current-user NSIS, WebView2-skip, portable package, company-PC check,
    release manifest, checksums, and GitHub Actions configuration.
12. Application, frontend, Tauri, Cargo, documentation, and packaging version
    consistency for 1.0.0-rc.1.
13. Single-instance, pre-upgrade backup, schema rejection, and close-guard
    source markers.

IMPORTANT SOURCE REVIEW
-----------------------
- Rapid edits are saved through one serialized queue; later snapshots cannot be
  overtaken by older in-flight writes.
- A failed snapshot stays pending and the top bar exposes Retry.
- Desktop database load errors show recovery controls and never substitute
  starter data.
- A second desktop launch focuses the existing instance.
- Existing workspaces receive a one-time Before_Upgrade backup when the stored
  application version changes.
- Unsupported schema versions fail closed.
- Backup, restore, import, and export flush pending edits first.

NOT EXECUTED IN THIS ENVIRONMENT
--------------------------------
- Rust/Cargo compilation.
- Native Windows Tauri executable compilation.
- NSIS setup generation.
- Portable Windows executable launch.
- Standard-user installation without elevation.
- Real SQLite persistence, backup, restore, and upgrade on Windows.
- Launch-at-sign-in and single-instance behavior on Windows.
- Uninstall/reinstall data-retention behavior.
- Company AppLocker, WDAC, SmartScreen, antivirus, WebView2, or endpoint-policy
  acceptance.
- Code-signing review.

Those checks are delegated to the Windows GitHub Actions build and
`docs/RC_ACCEPTANCE_CHECKLIST.md`. This is a release candidate, not the final
v1.0.0 production release, until the native Windows artifact and target-PC
critical data-safety checks pass.
